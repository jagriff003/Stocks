.venv/*
.venv-1/*
__pycache__/
*.pyc

# Generated data and reports
*.xlsx
*.csv
.price_cache/
charts/
live/

# ...but the universe and its dated snapshots are source, not output.
# They stay tracked so universe changes show up as reviewable diffs.
!universe.csv
!snapshots/universe/*.csv

# Pool snapshots are the point-in-time record that closes the survivorship
# question (TODO 0d).  They are only worth anything if they accumulate, so they
# are source rather than output and must show up in diffs.
!snapshots/pool/*.csv

# Compliance list must be tracked: changes to it have to show in a diff.
!restricted.csv

# The market store and the built long-history panel are what the hedge layer
# decides on (TODO 0l).  Tracked so a revised history shows up as a diff rather
# than as a silently different backtest.  The raw research downloads are
# binary and large; they stay on disk (vintaged) but out of git.
!data/market/*.csv
!data/longhistory/*.csv
data/longhistory/raw/
*.tmp

# Personal records: never published.  The folder is backed up elsewhere.
#   decision log   what was done with each recommendation
#   update_log     when the store was refreshed (run history)
#   private/       account-level material: reconciliations, trade exports
# Listed after the data/market negation above so it wins.
data/decisions/
data/market/update_log.csv
private/
